<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.web.place.mapper.RestaurantMapper">

	<select id="restaurantListData" resultType="con.sist.web.vo.PlaceVO" parameterType="hashmap">
		SELECT pno, name, addr, type, thumbnail, category
		FROM place
		WHERE category = #{category}
		OFFSET #{start} ROWS FETCH 16 ROWS ONLY
	</select>
	
	<select id="restaurantTotalPage" resultType="int" parameterType="string">
		SELECT CEIL(COUNT(*)/8.0) 
		FROM place
		WHERE category = #{category}
	</select>
</mapper>